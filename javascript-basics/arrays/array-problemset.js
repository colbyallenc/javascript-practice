// printReverse()
// Write a function that takes a single argument that is 
// assumed to be an array. Print that array, one line at 
// a time, in reverse order.

var array = [1,2,3,4,5];

array.forEach(function(arg){
// create a loops that moves backwards through the array
    for(var i; i <= array.length; i--){

    }


// return the array
    return array
});


// should probably use a loop



// isUniform()
// write a function which takes an array as an argument 
// and returns true if all elements in the array are identical
// store init



// sumArray()
// write a function sumArray() that accepts an array of 
// numbered and returns the sum of all numbers in the array

// store result


// max()
// function that accepts an array of numbers and returns 
// the maximum number in the array

// store max